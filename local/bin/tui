#!/bin/bash

[[ $1 == -* ]] && shift

if [[ $# == 0 ]]; then
    st ||
    xterm &
elif [[ $1 == /* ]]; then
    st -e "$@" ||
    xterm -e "$@" &
else
    s=${1%% *}
    set -- "printf \"\033]0;${s^}\a\"; $@"
    st -e $SHELL -ic "$*" ||
    xterm -e $SHELL -ic "$*" &
fi